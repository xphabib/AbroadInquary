<!-- ======================= TABLES =======================-->
<div class="row u-mt-xlarge#">
  <div class="col-12">
    <h3 class="u-mb-small">
      <%= link_to 'Create Student Application', new_student_application_path, class: 'c-badge c-badge--small# c-badge--info' if current_user.mentor?%>
      <%= 'Your Applications' if current_user.student? %>
    </h3>
  </div>
</div>

<%= render 'table_for_head_admin' if current_user.head_admin?%>
<%= render 'table_for_mentor' if current_user.mentor?%>
<%= render 'table_for_student' if current_user.student?%>